<div class="modal fade larger-modal" id="new-guide-modal">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title"><%= "New Guide for #{course.course_code}" %></h4>
      </div>
      <div class="modal-body">
        <div id="guide-error-container">
          <%= render 'shared/errors', record: guide %>  
        </div>

        <%= form_for guide, remote: true, html: { multipart: true } do |f| %>
        <div class="input">
          <%= f.text_field :title, placeholder: "Enter the guide's title", id: 'title', class: 'text_field' %>
          <label for="title">Title</label>
        </div>
        <div class="control-group">
          <%= f.label :content, :class => 'control-label' %>
          <div class="controls">
            <%= f.text_area :content, class: 'froala-textarea', id: 'guide-content' %>
          </div>
          <%= f.hidden_field :course_id, value: course.id %>
        </div>

        <div class="control-group">
          <%= f.label :attachments, :class => 'control-label' %>
          <div class="controls">
            <!-- GUIDE ATTACHMENTS UPLOAD -->
            <%= file_field_tag "uploads[]", type: :file, multiple: true %>
          </div>
        </div>
      </div>
      <div class="modal-footer">
        <%= f.submit 'Submit Guide', class: 'btn btn-primary', id: 'guide-submit' %>
      </div>



      <% end %>
    </div><!-- /.modal-content -->
  </div><!-- /.modal-dialog -->
</div><!-- /.modal -->
        
