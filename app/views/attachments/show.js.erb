<% if @attachments.any? %>
  $('.a-pag').html("<center><%= j paginate @attachments, remote: true, param_name: 'apage' %></center>");
  $('#other-attachments').html("<%= j render 'shared/attachments', attachments: @attachments, favourite: false %>");
<% end %>